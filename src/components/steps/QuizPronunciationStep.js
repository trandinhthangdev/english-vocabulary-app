import React from "react";
const QuizPronunciationStep = (props) => {
    // const handleSpeechRecognition = () => {
    //     const recognition = new window.webkitSpeechRecognition(); // For Chrome/Edge
    //     // const recognition = new window.SpeechRecognition(); // For Firefox
    //
    //     recognition.onresult = (event) => {
    //         const currentTranscript = event.results[0][0].transcript;
    //         setTranscript(currentTranscript);
    //     };
    //
    //     recognition.start();
    // };
    return (
        <div>
            QuizPronunciationStep
        </div>
    )
}


export default QuizPronunciationStep;

